services:
  - type: web
    name: enterprise-integration-hub
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn hub.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
